# Todo

- [x] Add command option to run: 
  - snake test (with memory-mapped video and its controls, + DEBUG=1)
  - nestest (with execution starting at 0xC000, + DEBUG=1)
  - create running abstractions first (possibility to overwrite program counter at startup)
- [ ] Complete the `debug_cpu_status` function
- [ ] Debug and fix CPU (comparing nestest outputs)